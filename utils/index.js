var formatArray = require('./formatArray');

module.exports = {
  formatArray: {
    transformToArrayOfStrings: formatArray.transformToArrayOfStrings,
    convertString: formatArray.convertString,
    convertBoolean: formatArray.convertBoolean,
    convertObject: formatArray.convertObject,
    convertNumber: formatArray.convertNumber,
    convertArray: formatArray.convertArray
  }
};
